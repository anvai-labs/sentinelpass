# Native libs (script-populated, NOT committed)

This directory holds the compiled Rust bridge static libraries that the
SentinelPassApp and SentinelPassCredential targets link against:

    libsentinelpass_mobile_bridge_ios_sim.a   (iphonesimulator)
    libsentinelpass_mobile_bridge_ios.a       (iphoneos device)

ADR-009 rev 2: committed `.a`/xcframework binaries are deleted — consumed
artifacts are generated by `ios/SentinelPass/build-ios.sh` (which wraps
`cargo build --package sentinelpass-mobile-bridge` for the iOS targets).
Run it once after cloning before building in Xcode. The `.a` files are
git-ignored (see `ios/SentinelPass/.gitignore`); only this placeholder and
the README are tracked.
